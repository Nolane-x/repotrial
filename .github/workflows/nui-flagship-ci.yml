name: NUI Flagship Website CI

on:
  push:
    branches:
      - build/nui-flagship-ci

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: nui-flagship-site
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Unit and geometry contracts
        run: npm test

      - name: M3 source wiring contract
        run: node tests/m3.source.cjs

      - name: M3 CSS containment contract
        run: node tests/m3.css.cjs

      - name: Production build
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Browser and render evidence
        run: npm run test:e2e

      - name: Package M3 milestone
        if: always()
        run: zip -q -r /tmp/NUI-Flagship-Website-M3.zip . -x 'node_modules/*' '.next/*'

      - name: Upload M3 evidence and ZIP
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nui-flagship-m3
          path: |
            nui-flagship-site/test-results
            nui-flagship-site/playwright-report
            /tmp/NUI-Flagship-Website-M3.zip
          if-no-files-found: warn
          retention-days: 30
